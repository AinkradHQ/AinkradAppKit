.build/
# xcodebuild -derivedDataPath scratch (a plugin repo's `make build` writes here)
build/
*.xcodeproj
.DS_Store

# Xcode/SwiftPM per-user state
.swiftpm/

# ABI digester scratch output (baseline itself is committed)
abi/report.txt
abi/report-signal.txt
